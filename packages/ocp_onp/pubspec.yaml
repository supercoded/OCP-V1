name: ocp_onp
description: ONP network layer — peer discovery and routing stats.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.16.0
  ocp_odp:
    path: ../ocp_odp

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.8
