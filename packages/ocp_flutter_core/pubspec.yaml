name: ocp_flutter_core
description: Shared Flutter theme and utilities for OCP‑V1.
version: 0.1.0

environment:
  sdk: ^3.19.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
