name: ocp_transport
description: Transport abstractions for BLE, USB, Serial, TCP, and mock.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.16.0
  ocp_odp:
    path: ../ocp_odp

dev_dependencies:
  test: ^1.25.8
  flutter_lints: ^5.0.0
