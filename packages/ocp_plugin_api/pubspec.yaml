name: ocp_plugin_api
description: Plugin SDK contracts for device, UI, and widget plugins.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.8
  flutter_lints: ^5.0.0
