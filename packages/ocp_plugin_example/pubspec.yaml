name: ocp_plugin_example
description: Example plugin validating the OCP plugin SDK contract.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  ocp_plugin_api:
    path: ../ocp_plugin_api

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.25.8
