name: ocp_bridge_meshtastic
description: Meshtastic protobuf bridge to ODP frames.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.5.0

dependencies:
  ocp_odp:
    path: ../ocp_odp

dev_dependencies:
  test: ^1.25.8
  flutter_lints: ^5.0.0
