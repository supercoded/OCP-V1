android {
    compileSdkVersion 34
    namespace "com.ocp.ocp_v1"

    defaultConfig {
        applicationId "com.ocp.ocp_v1"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "0.1.0"
    }

    buildTypes {
        release {
            // TODO: Add signing config for release builds
            minifyEnabled false
        }
    }
}
