name: ocp_v1
description: Operational Comms Platform - Cross-platform field operations
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  shared_preferences: ^2.2.2
  ocp_flutter_core:
    path: ../../packages/ocp_flutter_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true